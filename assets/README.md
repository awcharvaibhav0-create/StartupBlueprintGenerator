# Assets Directory

Place any static assets here:

- `logo.png` — custom logo displayed in the Streamlit sidebar
- `favicon.ico` — browser favicon override
- Any images referenced in the UI

The application works without any files in this directory.
